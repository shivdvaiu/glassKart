import 'package:flutter/material.dart';
import 'package:sizer/sizer.dart';

var btnBorderRadius = Radius.circular(20.h);
// ---- Vertical Spacing ----
var xxsHeightSpan = SizedBox(height: 1.h);
var xsHeightSpan = SizedBox(height: 2.h);
var sHeightSpan = SizedBox(height: 4.h);
var mHeightSpan = SizedBox(height: 16.h);
var lHeightSpan = SizedBox(height: 24.h);
var elHeightSpan = SizedBox(height: 48.h);

// ---- Horizontal Spacing ----
var xxsWidthSpan = SizedBox(width: 2.w);
var xsWidthSpan = SizedBox(width: 4.w);
var sWidthSpan = SizedBox(width: 8.w);
var mWidthSpan = SizedBox(width: 16.w);
var lWidthSpan = SizedBox(width: 24.w);
var elWidthSpan = SizedBox(width: 48.2);

// ---- Page Paddings ----
var lPagePadding = EdgeInsets.symmetric(horizontal: 64.w);
var mPagePadding = EdgeInsets.symmetric(horizontal: 32.w);
var mXPagePadding = EdgeInsets.symmetric(horizontal: 32.h);
var mYPagePadding = EdgeInsets.symmetric(vertical: 32.h);
var sXPagePadding = EdgeInsets.symmetric(horizontal: 16.w);
var sYPagePadding = EdgeInsets.symmetric(vertical: 16.h);
var sPagePadding = EdgeInsets.all(16);

// ---- Container Padding ----
var sPadding = EdgeInsets.symmetric(horizontal: 8.w, vertical: 8.h);
var sXPadding = EdgeInsets.symmetric(horizontal: 10.w);
var xsYPadding = EdgeInsets.symmetric(vertical: 4.h);
var sYPadding = EdgeInsets.symmetric(vertical: 8.h);
var mPadding = EdgeInsets.symmetric(horizontal: 12.w, vertical: 12.h);
var mXPadding = EdgeInsets.symmetric(horizontal: 12.w);
var mYPadding = EdgeInsets.symmetric(vertical: 12.h);
var lPadding = EdgeInsets.symmetric(horizontal: 16.w, vertical: 18.h);
var lXPadding = EdgeInsets.symmetric(horizontal: 18.w);
var lYPadding = EdgeInsets.symmetric(vertical: 18.h);

var xlPadding = EdgeInsets.symmetric(horizontal: 48.w, vertical: 10.h);
var xlXPadding = EdgeInsets.symmetric(horizontal: 25.w);
var xlYPadding = EdgeInsets.symmetric(vertical: 24.h);
